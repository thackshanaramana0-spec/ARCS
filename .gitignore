# ── Build artifacts (regenerated by cmake) ──
build/
build_wsl/
build_win/
build_test/
cmake-build-*/
*.o
*.obj
*.a
*.so
*.exe
arcs_cpp/

# ── Archives & data (never commit — fetch test data separately) ──
*.arcs
*.fq
*.fastq
*.fastq.gz
*.fq.gz
*.fa
*.fasta
*.bam
*.bai
*.bam.bai
*.sam
*.cram
*.vcf
*.vcf.gz
*.bed
*.h5
*.histo
*.spring
*.genozip

# ── Scratch / editor ──
*.log
*.tmp
*.dec
.DS_Store
*.swp
__pycache__/

# Third-party PgRC2 (GPL-3), cloned for benchmarking only — never vendored
# into this repo. Reproduce with:
#   git clone https://github.com/kowallus/PgRC.git method_c
#   cd method_c && mkdir build && cd build && cmake .. && make PgRC
method_c/
